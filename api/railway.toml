[build]
builder = "NIXPACKS"

[deploy]
startCommand = "bun run build && bun start"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
